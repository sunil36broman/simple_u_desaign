export interface Product {
    id: string;
    name: string;
    email: string;
    body: string;
  }
  
  export interface Category {
    id: string;
    title: string;
    body: string;
    // products: Product[];
  }
  